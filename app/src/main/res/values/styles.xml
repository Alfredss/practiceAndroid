<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!--Estilo para mi Edit Text de la actividad MainEstilosActivity-->
    <style name="numbers">
        <item name="android:inputType">date</item>
        <item name="android:hint">Ingrese Fecha</item>
        <item name="android:width">200dp</item>
        <item name="android:height">70dp</item>
        <item name="android:colorBackground">@android:color/holo_orange_dark</item>
    </style>

    <!--Tema Propio para la actividad MainTemasActivity-->


</resources>
